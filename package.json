{
  "name": "sc-engineering-vanilla",
  "version": "1.0.0",
  "scripts": {
    "setup": "node setup.js",
    "dev": "vite src",
    "build": "vite build src --outDir ../dist --emptyOutDir",
    "preview": "vite preview --outDir dist"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}